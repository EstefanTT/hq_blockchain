# apps/spread-bot/

Runs the spread-capture strategy on configured DEX pools. Monitors spreads and executes small trades.

**Enable**: uncomment in `index.js`. Configure pools and parameters in `config.json`.
