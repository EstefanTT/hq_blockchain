# apps/portfolio-monitor/

Tracks wallet balances and positions across all configured chains. Periodically snapshots portfolio state.

**Enable**: uncomment in `index.js` or add to `scheduler/crons/`.
