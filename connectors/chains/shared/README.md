# connectors/chains/shared/

Shared chain adapter utilities — base interface definition, common types, cross-chain helpers.

`baseAdapter.js` defines the interface that all chain adapters should implement.
