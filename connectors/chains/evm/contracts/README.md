# connectors/chains/evm/contracts/

Contract ABI definitions and typed interaction helpers for EVM chains.

Store ABIs as JSON files. Create wrapper functions for common contract interactions (ERC-20 approve/transfer, router swaps, etc.).
